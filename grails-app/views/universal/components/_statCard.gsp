<div class="bg-white rounded-xl shadow-sm border border-stone-200 p-4">
    <div class="text-2xl font-bold text-stone-800">${value}</div>
    <div class="text-xs text-stone-500 mt-1">${label}</div>
    <g:if test="${hint}">
        <div class="text-xs text-stone-400 mt-0.5">${hint}</div>
    </g:if>
</div>
